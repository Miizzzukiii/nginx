apiVersion: v1
kind: ConfigMap
metadata:
  name: #через values
  labels:
    app: {{ .Chart.Name }}
data:
  nginx.conf: |
    events {}
    http {
      server {
        listen 80;
        location / {
          proxy_pass http://example.com;
        }
      }
    }

